{
    "configurations": [
        {
            "name": "Start tailwind server",
            "type": "node-terminal",
            "request": "launch",
            "command": "npx tailwindcss -i css/styles.css -o css/tailwind.css --watch"
        }
    ]
}